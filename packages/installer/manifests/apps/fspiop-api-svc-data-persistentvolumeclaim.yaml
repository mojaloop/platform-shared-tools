apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    mojaloop.service: fspiop-api-svc-data
    mojaloop.layer: apps
  name: fspiop-api-svc-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    mojaloop.layer: apps
    mojaloop.service: quoting-command-handler-svc
  name: quoting-command-handler-svc
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  selector:
    matchLabels:
      mojaloop.service: quoting-command-handler-svc
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        mojaloop.layer: apps
        mojaloop.service: quoting-command-handler-svc
    spec:
      containers:
      - env:
        - name: CONSUMER_BATCH_SIZE
          value: "150"
        - name: CONSUMER_BATCH_TIMEOUT_MS
          value: "5"
        envFrom:
        - configMapRef:
            name: vnext-configs
        image: mojaloop/quoting-bc-command-handler-svc:0.6.2
        image: mojaloop/quoting-bc-command-handler-svc:0.6.2
        imagePullPolicy: IfNotPresent
        livenessProbe:
          exec:
            command:
            - sh
            - -c
            - nc -z localhost 3035
          failureThreshold: 10
          initialDelaySeconds: 30
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 10
        name: quoting-command-handler-svc
        ports:
        - containerPort: 3035
          protocol: TCP
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
